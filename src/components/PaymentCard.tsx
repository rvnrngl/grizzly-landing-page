import React from "react";
import paymentCard from "../assets/paymentCard.svg";
const PaymentCard: React.FC = () => {
  return <img id="payment-details" src={paymentCard} alt="payment card" />;
};

export default PaymentCard;
